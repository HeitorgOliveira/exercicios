#include <iostream>
#include <string.h>

int main()
{
    bool passou = false;
    int n, t;
    std::string a,b,c;
    std::cin >> t;
    for (int i = 0; i < t; i++)
    {
        std::cin >> n;
        std::cin >> a;
        std::cin >> b;
        std::cin >> c;
        for (int j = 0; j < n; j++)
        {
            //if 
            if (a[i] == c[i] || b[i] == c[i])
            {
                
            }
        }
    }
}